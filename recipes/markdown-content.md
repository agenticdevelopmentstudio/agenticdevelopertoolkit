---
id: 2d18fdbd-b2c5-46c0-8e6f-ceb7467fafa6
title: Markdown Content
domain: agenticdevelopertoolkit://recipes/markdown-content
type: ingredient
version: 1.1.0
status: review
language: en
created: 2026-09-22
modified: 2026-09-22
author: Mike Fullerton
copyright: 2026 Mike Fullerton
license: MIT
summary: Server-side markdown processor renders sanitized HTML with configurable styling.
platforms:
- typescript
- web
tags:
- markdown
- prose
- content-rendering
depends-on: []
related:
- agenticdevelopertoolkit://recipes/markdown-renderer
references:
- https://unifiedjs.com
- https://github.com/rehypejs/rehype-sanitize
- https://shiki.style
approved-by: ''
approved-date: ''
---

# Markdown Content

## Overview

A React Server Component that processes raw markdown content into sanitized, syntax-highlighted HTML at build or request time. The component renders the HTML output with a base prose styling class and optional additional classes. Use this to server-render markdown in static or server-prerendered routes where content is available at build or request time.

## Behavioral Requirements

- **render-markdown**: The component MUST process raw markdown input through the same unified pipeline used by the client `MarkdownRenderer` component (parse → GFM → remark-to-rehype with raw HTML disabled → heading slugs/autolinks → syntax highlighting → sanitize → stringify), so a document renders identically whichever path a host uses. (Web implementation: `processMarkdown`; see Platform Notes.)
- **sanitize-output**: The component MUST sanitize the generated HTML with an explicit tag/attribute/protocol allowlist — not a denylist — before it reaches the DOM, so markdown input cannot execute script, run event handlers, or navigate via a `javascript:` URL. Sanitization MUST run *after* syntax highlighting, since highlighting produces real element nodes (not raw HTML) for the sanitizer to filter; running it first would strip the highlighter's token markup. (See Compliance.)
- **highlight-code**: The component MUST syntax-highlight fenced code blocks before sanitization runs, using a highlighter instance that is created once and cached at module scope, so repeated calls in the same process reuse it (see Design Decisions).
- **apply-prose-class**: The component MUST always apply the `adh-mv-prose` CSS class to the root container.
- **accept-optional-class**: The component MUST accept an optional `className` prop and append it to the root class list when provided.
- **server-component-only**: The component MUST be an async React Server Component (RSC) — no `"use client"` directive.
- **strip-frontmatter**: The component MUST strip a leading YAML frontmatter block — one starting with `---` at position 0 of the raw content, through its matching closing `---` — before processing. A `---` occurring anywhere else in the document is ordinary markdown and MUST render as a thematic break (`<hr>`), not be treated as frontmatter.
- **output-root-div**: The component MUST render output in a root `<div>` element with the combined classes.
- **propagate-render-errors**: If the markdown pipeline throws (malformed input the parser cannot recover from, or a highlighter/grammar failure), the component MUST let the error propagate to the nearest error boundary. It MUST NOT catch the error itself, render partial output, or fall back to unsanitized HTML.

## Appearance

- **Root container**: `<div>` with class `adh-mv-prose`
- **Styling source**: CSS class `adh-mv-prose` defines typography, spacing, and prose styling; same styling as the client `MarkdownRenderer` component for consistent output across server and client rendering paths.
- **Additional styling**: Optional `className` prop value is appended to the root class list, allowing scoped style overrides.
- **HTML structure**: Output is semantic HTML generated by the markdown processor (headings, paragraphs, lists, code blocks, links, etc.).

## States

Not applicable: the component is a static presentation element with no interactive states.

## Accessibility

The component delegates accessibility to the semantic HTML the pipeline produces: real heading elements (`h1`–`h6`), list structures, table markup, and code block markers, plus rehype-slug/rehype-autolink-headings anchor ids for in-page navigation. Heading levels are exactly whatever the source markdown authored — the pipeline does not renumber, offset, or demote them, so a `# ` in the markdown always becomes an `<h1>`. A host page that already renders its own `<h1>` MUST start the markdown source at `##` or lower to avoid a duplicate or mis-nested top-level heading; this component has no `headingOffset` option to do that automatically. Content authors are responsible for the rest of accessible markdown authoring — alt text in image syntax, descriptive link text, and correct heading order within the document. See **highlight-code**: shiki's output is inert `<span>` token markup with no ARIA role, so it carries no additional accessibility burden.

## Conformance Test Vectors

| ID | Requirements | Input | Expected |
|----|-------------|-------|----------|
| markdown-001 | render-markdown, sanitize-output, apply-prose-class | `{ content: "# Hello\n\nWorld" }` | Root `<div class="adh-mv-prose">` containing an `<h1>` whose text content is `Hello` and a `<p>` whose text content is `World`. Comparison ignores whitespace between tags and the `id`/anchor-`<a>` wrapper that rehype-slug and rehype-autolink-headings add to every heading — this normalization applies to every vector below that renders a heading. |
| markdown-002 | accept-optional-class, apply-prose-class | `{ content: "text", className: "custom-override" }` | Root `<div>` class attribute is exactly `"adh-mv-prose custom-override"` |
| markdown-003 | strip-frontmatter | `{ content: "---\nfrontmatter: value\n---\n# Title" }` | `<div class="adh-mv-prose"><h1>Title</h1></div>` — the leading `---…---` block and its contents do not appear anywhere in the output |
| markdown-004 | sanitize-output | `{ content: "<script>alert('xss')</script>" }` | Output contains no `<script` substring and no `on`-prefixed event-handler attribute |
| markdown-005 | highlight-code | `` { content: "```ts\nconst x = 1\n```" } `` | Output contains a `<pre class="shiki …">` element (class list includes `shiki`) whose descendant `<code>`/`<span>` elements carry the highlighted tokens. Comparison ignores the exact token boundaries and the `--shiki-light`/`--shiki-dark` CSS variable values, which are theme data. |
| markdown-006 | strip-frontmatter | `{ content: "# Title\n\n---\n\nMore text" }` | The `---` MUST render as an `<hr>` element between the two paragraphs — it is a thematic break, not frontmatter, because it does not start at position 0 of the content |
| markdown-007 | output-root-div, apply-prose-class | `{ content: "   \n\n  " }` | `<div class="adh-mv-prose"></div>` — an empty root, no error thrown |
| markdown-008 | output-root-div, apply-prose-class | `{ content: "" }` | `<div class="adh-mv-prose"></div>` — an empty root, no error thrown |
| markdown-009 | accept-optional-class | `{ content: "text" }` (no `className`) | Root `<div>` class attribute is exactly `"adh-mv-prose"` — no trailing space, no literal `undefined`/`null` |
| markdown-010 | render-markdown, highlight-code | `{ content: "- a\n  - b\n\n\| A \| B \|\n\|---\|---\|\n\| 1 \| 2 \|\n\n```js\nconst y = 2\n```\n\n[link](https://example.com)" }` | Renders without throwing: a nested `<ul>`, a `<table>`, a shiki-highlighted `<pre class="shiki …">`, and an `<a href="https://example.com">` |

## Edge Cases

- **Empty content**: When `content` is an empty string, the component MUST render an empty `<div class="adh-mv-prose"></div>` (markdown-008).
- **Whitespace-only content**: When `content` contains only whitespace, the component MUST render the same empty `<div class="adh-mv-prose"></div>`, with no error thrown (markdown-007).
- **Complex markdown**: Nested lists, tables, code blocks, inline code, and links MUST all be processed and rendered without error (markdown-010).
- **Missing className prop**: When `className` is not provided or undefined, the component MUST render with only the `adh-mv-prose` class (no undefined or null appended) (markdown-009).

## Configuration

| Option | Type | Required | Default | Description |
|--------|------|----------|---------|-------------|
| `content` | string | Yes | — | Raw markdown string. A leading YAML frontmatter block (`---` at the very start of the string through the matching closing `---`) is stripped before processing; a `---` appearing later in the document is an ordinary thematic break and renders as `<hr>` (see **strip-frontmatter**). |
| `className` | string | No | undefined | Additional CSS class(es) appended to the root `<div>` class list (see **accept-optional-class**). |

## Deep Linking

Not applicable: this is a content rendering component with no navigation affordances or URL routing.

## Localization

Not applicable: the component does not generate user-facing UI strings. Content localization is the responsibility of the markdown source.

## Accessibility Options

Not applicable: the component does not respond to accessibility display options; its output is semantic HTML whose behavior is controlled by the platform's assistive technology and browser.

## Feature Flags

Not applicable: the component has no conditional behavior or optional features controlled by flags.

## Analytics

Not applicable: the component does not emit or collect analytics events.

## Privacy

Not applicable: the component does not collect, transmit, or store user data.

## Logging

Not applicable: the component does not emit diagnostic logs.

## Platform Notes

- **Web (React)**: This is the source platform. The component is a Server Component in `packages/web/packages/markdown/src/components/MarkdownContent.tsx`. It is async and calls the shared `processMarkdown` function from `lib/process-markdown`, which runs the unified pipeline: remark-parse → remark-gfm → an ADH alert transform → remark-rehype (`allowDangerousHtml: false`, so embedded raw HTML/`<script>` is dropped, not passed through) → rehype-slug + rehype-autolink-headings → shiki highlighting (via `@shikijs/rehype/core` against a module-cached highlighter) → rehype-sanitize (a custom allowlist schema, not the library default) → rehype-stringify. The HTML is included in the SSR payload, making it crawlable and eliminating client-side rendering flash. The root element always includes class `adh-mv-prose`; a caller-supplied `className` is appended with a single space.

  None of the platforms below has a build-time or server-component rendering equivalent, so each fetches the already-processed HTML from a server endpoint (this component's output, or an equivalent API) rather than re-implementing the markdown pipeline natively.

- **SwiftUI**: Render the fetched HTML in a `WebView` (SwiftUI, iOS/macOS 26+) with the `adh-mv-prose` CSS injected — the option with full parity (tables, task-list checkboxes, shiki-highlighted code). For markdown simple enough to skip code blocks/tables/images, `Text(AttributedString(markdown:))` is a lighter-weight native alternative, but it does not reproduce this pipeline's syntax highlighting or table rendering.

- **Compose**: Render the fetched HTML in an `AndroidView` wrapping a `WebView`, with the `adh-mv-prose` CSS (or its Android-equivalent spacing and typography) injected. There is no Compose-native markdown-to-Composable library that reproduces GFM tables, task-list checkboxes, and shiki-highlighted code with parity to the web output; treat native-Composable rendering as a separate, lower-fidelity capability outside this recipe's scope.

- **AppKit / UIKit**: On macOS, use `WKWebView` with the injected CSS. On iOS, use `WKWebView` as well — there is no first-party native markdown/attributed-text API that reproduces GFM tables, task lists, and syntax-highlighted code. Reserve a native-text fallback (translating `adh-mv-prose` typography — font size, line height, paragraph spacing) for content simple enough not to need `WKWebView`.

- **WinUI 3**: Use a `WebView2` control's `NavigateToString()` to load the fetched HTML with the `adh-mv-prose` CSS (or WinUI-equivalent Fluent typography and spacing) injected inline. `NavigateToString` has a documented content-length ceiling (~2 MB); for longer documents, host the HTML via `CoreWebView2.SetVirtualHostNameToFolderMapping` or navigate `WebView2` directly to the server URL instead of inlining it.

## Design Decisions

**Decision**: The component is async and server-side-only (a React Server Component), with no client-rendering path.
**Rationale**: Trades client-side flexibility (content cannot be chosen dynamically on the client without a server round-trip) for a crawlable, SEO-friendly HTML payload and no client-side "Rendering…" flash.
**Approved**: pending

**Decision**: The `adh-mv-prose` class is always applied to the root; an optional caller-supplied `className` is appended rather than replacing it.
**Rationale**: Guarantees consistent prose styling across every rendering (server and client paths look identical) while still letting a consumer scope additional style overrides.
**Approved**: pending

**Decision**: The shiki highlighter and the frozen unified processor are built once and cached at module scope (see **highlight-code**).
**Rationale**: Avoids redundant highlighter initialization on every request; repeated calls in the same process reuse the same instances.
**Approved**: pending

## Compliance

| Check | Status | Category |
|-------|--------|----------|
| [input-sanitization](agenticdevelopercookbook://compliance/security#input-sanitization) | passed | Security |
| [semantic-markup](agenticdevelopercookbook://compliance/accessibility#semantic-markup) | passed | Accessibility |
| [screen-reader-support](agenticdevelopercookbook://compliance/accessibility#screen-reader-support) | partial | Accessibility |
| [dynamic-type-support](agenticdevelopercookbook://compliance/accessibility#dynamic-type-support) | passed | Accessibility |
| [contrast-ratio](agenticdevelopercookbook://compliance/accessibility#contrast-ratio) | partial | Accessibility |
| [unicode-support](agenticdevelopercookbook://compliance/internationalization#unicode-support) | passed | Internationalization |

`input-sanitization`, `semantic-markup`, `dynamic-type-support`, and `unicode-support` rest on `process-markdown.ts` (a custom rehype-sanitize allowlist run after shiki, remark-rehype's semantic element output, `rem`-based prose CSS, and native JS string handling throughout); `screen-reader-support` and `contrast-ratio` are `partial` because accessible names for links/images and the full prose color palette depend on the markdown author and on `themes/palettes.ts`, neither of which this component controls.

## Change History

| Version | Date | Author | Summary |
|---------|------|--------|---------|
| 1.1.0 | 2026-09-22 | Mike Fullerton | Lint pass: rename requirements to subject-only kebab-case; correct the SwiftUI/Compose/WinUI platform APIs; add highlight-code and propagate-render-errors requirements; add exact/deterministic test vectors for frontmatter-vs-thematic-break, whitespace-only, and code highlighting; reformat Design Decisions into Decision/Rationale/Approved entries; add the Compliance table; populate related and references |
| 1.0.0 | 2026-09-22 | Claude Haiku 4.5 | Initial creation |
